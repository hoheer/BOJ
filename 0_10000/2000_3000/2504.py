from sys import stdin
from collections import deque


def solution(c, n):

    return 1
bk_s = ["(", "["]
str = stdin.readline().rstrip()
    for c in str:
        if c in bk_s:
            solution(c, n)
